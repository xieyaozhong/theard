const canvas=document.getElementById('field');const ctx=canvas.getContext('2d');let w=0,h=0,dpr=1;const pointer={x:-1e4,y:-1e4};const points=[];function resize(){dpr=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;canvas.width=w*dpr;canvas.height=h*dpr;canvas.style.width=w+'px';canvas.style.height=h+'px';ctx.setTransform(dpr,0,0,dpr,0,0);points.length=0;const count=Math.min(100,Math.floor(w*h/16000));for(let i=0;i<count;i++)points.push({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.16,vy:(Math.random()-.5)*.16,r:Math.random()*1.2+.2})}addEventListener('resize',resize);addEventListener('pointermove',e=>{pointer.x=e.clientX;pointer.y=e.clientY});addEventListener('pointerleave',()=>{pointer.x=-1e4;pointer.y=-1e4});function frame(){ctx.clearRect(0,0,w,h);for(const p of points){p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>w)p.vx*=-1;if(p.y<0||p.y>h)p.vy*=-1;const dx=p.x-pointer.x,dy=p.y-pointer.y,dist=Math.hypot(dx,dy);if(dist<150&&dist>0){p.x+=dx/dist*.8;p.y+=dy/dist*.8}ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle='rgba(199,255,55,.28)';ctx.fill()}requestAnimationFrame(frame)}resize();frame();const observer=new IntersectionObserver(entries=>{for(const entry of entries)if(entry.isIntersecting)entry.target.classList.add('visible')},{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
