const $=(s,p=document)=>p.querySelector(s);const $$=(s,p=document)=>[...p.querySelectorAll(s)];
const boot=$('#boot');setTimeout(()=>boot?.classList.add('done'),1500);
const menu=$('#menu'),openBtn=$('#menuBtn'),closeBtn=$('#menuClose');
function setMenu(open){menu?.classList.toggle('open',open);menu?.setAttribute('aria-hidden',String(!open));document.body.classList.toggle('menu-open',open)}
openBtn?.addEventListener('click',()=>setMenu(true));closeBtn?.addEventListener('click',()=>setMenu(false));$$('.menu__nav a').forEach(a=>a.addEventListener('click',()=>setMenu(false)));addEventListener('keydown',e=>{if(e.key==='Escape')setMenu(false)});
const cursor=$('#cursor');addEventListener('pointermove',e=>{if(cursor){cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px'}});$$('a,button,.case__visual').forEach(el=>{el.addEventListener('pointerenter',()=>cursor?.classList.add('hot'));el.addEventListener('pointerleave',()=>cursor?.classList.remove('hot'))});
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('visible')}),{threshold:.12});$$('.scene__label,.statement__grid,.work__head,.case__grid,.case__meta,.live__grid,.ticket,.finale h2,.finale__bottom').forEach(el=>{el.classList.add('reveal');observer.observe(el)});
const drift=$('[data-drift]');const progress=$('#progress');let targetY=0,currentY=0;addEventListener('scroll',()=>{targetY=scrollY;const max=document.documentElement.scrollHeight-innerHeight;const p=max>0?scrollY/max:0;if(progress)progress.style.height=`${p*100}%`;document.documentElement.style.setProperty('--scroll-progress',p.toFixed(4))},{passive:true});
function driftFrame(){currentY+=(targetY-currentY)*.075;if(drift)drift.style.transform=`translate3d(${Math.min(currentY*.012,26)}px,${currentY*-.012}px,0)`;requestAnimationFrame(driftFrame)}driftFrame();
$$('.case__visual').forEach(panel=>{panel.addEventListener('pointermove',e=>{const r=panel.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;panel.style.setProperty('--rx',`${(-y*4.5).toFixed(2)}deg`);panel.style.setProperty('--ry',`${(x*6).toFixed(2)}deg`)});panel.addEventListener('pointerleave',()=>{panel.style.setProperty('--rx','0deg');panel.style.setProperty('--ry','0deg')})});
const matterCss=document.createElement('link');matterCss.rel='stylesheet';matterCss.href='matter.css';document.head.appendChild(matterCss);
const hero=$('.hero');if(hero){const readout=document.createElement('div');readout.className='matter-readout';readout.innerHTML='<span>THREE.JS / WEBGL / GLSL</span><b>INTERACTIVE MATTER CORE</b>';hero.appendChild(readout)}
import('./matter.js').catch(err=>{document.documentElement.classList.add('webgl-fallback');console.warn('THEARD WebGL enhancement unavailable; base experience remains active.',err)});