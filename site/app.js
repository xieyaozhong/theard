const boot=document.getElementById('boot');setTimeout(()=>boot.classList.add('done'),1550);
const menu=document.getElementById('menu'),menuBtn=document.getElementById('menuBtn'),menuClose=document.getElementById('menuClose');function toggleMenu(open){menu.classList.toggle('open',open);menu.setAttribute('aria-hidden',String(!open));document.body.style.overflow=open?'hidden':''}menuBtn.addEventListener('click',()=>toggleMenu(true));menuClose.addEventListener('click',()=>toggleMenu(false));menu.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>toggleMenu(false)));
const cursor=document.getElementById('cursor');addEventListener('pointermove',e=>{cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px'});document.querySelectorAll('a,button').forEach(el=>{el.addEventListener('mouseenter',()=>{cursor.style.width='42px';cursor.style.height='42px'});el.addEventListener('mouseleave',()=>{cursor.style.width='20px';cursor.style.height='20px'})});
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const drift=document.querySelector('.drift');addEventListener('scroll',()=>{if(drift)drift.style.transform=`translate3d(${Math.min(scrollY*.035,34)}px,0,0)`},{passive:true});
const canvas=document.getElementById('field'),ctx=canvas.getContext('2d');let w=0,h=0,dpr=1;const pointer={x:-9999,y:-9999};let pts=[];function resize(){dpr=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;canvas.width=w*dpr;canvas.height=h*dpr;canvas.style.width=w+'px';canvas.style.height=h+'px';ctx.setTransform(dpr,0,0,dpr,0,0);const count=Math.min(95,Math.max(34,Math.floor(w*h/19000)));pts=Array.from({length:count},()=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.2,vy:(Math.random()-.5)*.2,r:Math.random()*1.1+.25}))}addEventListener('resize',resize);addEventListener('pointermove',e=>{pointer.x=e.clientX;pointer.y=e.clientY});addEventListener('pointerleave',()=>{pointer.x=-9999;pointer.y=-9999});function frame(){ctx.clearRect(0,0,w,h);for(let i=0;i<pts.length;i++){const p=pts[i];p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>w)p.vx*=-1;if(p.y<0||p.y>h)p.vy*=-1;let dx=p.x-pointer.x,dy=p.y-pointer.y,d=Math.hypot(dx,dy);if(d<155&&d>0){p.x+=dx/d*1.15;p.y+=dy/d*1.15}ctx.fillStyle='rgba(214,255,69,.38)';ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill();for(let j=i+1;j<pts.length;j++){const q=pts[j],dd=Math.hypot(p.x-q.x,p.y-q.y);if(dd<105){ctx.strokeStyle=`rgba(214,255,69,${(1-dd/105)*.06})`;ctx.lineWidth=.6;ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(q.x,q.y);ctx.stroke()}}}requestAnimationFrame(frame)}resize();frame();